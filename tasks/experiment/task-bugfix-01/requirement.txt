修复 src/multi_agent/workspace.py 中 validate_outbox_data 函数的问题：
当 role 为 "reviewer" 时，函数只检查 "decision" 字段是否存在，
但没有检查 "summary" 字段。reviewer 输出也应该包含 summary。

实现要求:
1. 在 validate_outbox_data() 中，当 role == "reviewer" 时，增加对 "summary" 字段的检查
2. 如果 reviewer 输出缺少 "summary"，添加错误信息 "missing 'summary' field"
3. builder 的验证逻辑不变

不要修改函数签名。